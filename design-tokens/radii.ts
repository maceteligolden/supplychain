/**
 * Border radii — slightly scaled down from enterprise defaults.
 */

export const radii = {
  /** Buttons, inputs — 8px */
  control: "0.5rem",
  /** Cards, tables — 12px */
  card: "0.75rem",
  /** Base radius token for shadcn calc chain */
  base: "0.5rem",
} as const;
